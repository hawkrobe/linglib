import Linglib.Semantics.Gradability.Antonymy
import Linglib.Semantics.Gradability.AntonymPrediction
import Linglib.Pragmatics.Implicature.Markedness
import Linglib.Data.Examples.TesslerFranke2019
import Linglib.Phonology.Constraints.Defs
import Linglib.Phonology.OptimalityTheory.Basic
import Linglib.Pragmatics.Superoptimal

/-!
# [krifka-2007b] — Negated Antonyms: Creating and Filling the Gap
[krifka-2007b]

In:, Presupposition and Implicature in
Compositional Semantics, Palgrave Macmillan.

## Krifka's Thesis

Krifka argues, against the received view ([cruse-1986], [horn-1989]),
that antonyms like happy/unhappy are **literally contradictory** — they
exhaustively partition the scale with a single threshold. The gap between
"not unhappy" and "happy" arises through **pragmatic strengthening**, not
through the semantics of contrary negation.

### Three Hypotheses (§3)

1. **Epistemic vagueness**: Speakers avoid borderline cases to ensure safe
   communication (following Williamson 1994).
2. **Exhaustive antonymy**: happy and unhappy are contradictories — they
   literally exhaust their scale. Evidence: unconditionals like
   "Regardless whether you are happy or unhappy, you should read this book"
   (ex. 22) entail the predicate covers everyone.
3. **M-principle**: Of two expressions with similar meaning, the simpler one
   is restricted to stereotypical interpretations, the complex one to
   non-stereotypical interpretations ([horn-1984]).

### Central Argument

Under contradictory semantics (single θ), "not unhappy" = "happy" (DNE).
The M-principle breaks this synonymy: since "not unhappy" is more complex
than "happy" (5 vs 0 cost units), the complex form is pragmatically
restricted to the non-stereotypical region — the plateau/gap between clearly
happy and clearly unhappy.

### Quadruplet Structure (ex. 1)

Krifka's analysis centers on quadruplets:
  happy, not happy, unhappy, not unhappy
with form complexity: |happy| < |unhappy| < |not happy| < |not unhappy|

## Verification

Formalizes the quadruplet structure, proves the contradictory synonymy
puzzle and its resolution via ThresholdPair, and states transfer equations
against [tessler-franke-2019]'s quadruplet rows
(`Data/Examples/TesslerFranke2019.json`). The pragmatic mechanism connecting
contradictory base → effective ThresholdPair is derived via two routes:
1. **Bidirectional OT** (§ 9 below): [blutner-2000]'s weak BiOT (eq. 14)
   derives the four-way form-meaning assignment via the greatest-fixed-point
   computation in `Core.Optimization.Evaluation.superoptimal`.
2. **RSA model**: [tessler-franke-2019] (`Studies/TesslerFranke2020PMF.lean`)
   derives the same effect through Bayesian pragmatic reasoning.
-/

set_option autoImplicit false

namespace Krifka2007

open Semantics.Degree (Degree Threshold deg thr)
open Semantics.Gradability (ThresholdPair inGapRegion
  positiveMeaning' contraryNegMeaning notContraryNegMeaning contradictoryNeg
  AntonymForm contradictoryDenot_synonymy strengthenedDenot_breaks_synonymy)
open Semantics.Gradability.Antonymy
open Semantics.Degree (positiveMeaning)
open Data.Examples
open Features (NegationType)
open Pragmatics.Bidirectional (superoptimal superoptimalSet
  superoptimal_coe_eq_set Blocks)
open Constraints OptimalityTheory

-- ════════════════════════════════════════════════════
-- § 1. Quadruplet: The Central Data Structure
-- ════════════════════════════════════════════════════

/-! Krifka's quadruplet (ex. 1) — the four forms `{happy, not happy, unhappy,
    not unhappy}` generated by combining an antonym pair with sentential
    negation — is provided as substrate by
    `Semantics.Gradability.AntonymForm` (file
    `Semantics/Gradability/AntonymQuadruplet.lean`). The complexity
    ordering `0 < 2 < 3 < 5` lives there as `AntonymForm.complexity`. -/

-- ════════════════════════════════════════════════════
-- § 2. Contradictory Base: The Literal Semantics
-- ════════════════════════════════════════════════════

/-! Krifka's H2 — antonyms are literally contradictory (single threshold θ);
    all four forms derive from propositional operations on `d > θ` — is
    captured by substrate `AntonymForm.contradictoryDenot` (file
    `Semantics/Gradability/AntonymPrediction.lean`). The synonymy
    puzzle (negative ≡ notPositive, notNegative ≡ positive under contradictory
    semantics) is the substrate theorem `contradictoryDenot_synonymy`.

    See also `Antonymy.contradictory_dne` for the DNE form (the puzzle
    Krifka's pragmatic strengthening solves). -/

-- ════════════════════════════════════════════════════
-- § 3. Strengthened Semantics: Breaking Synonymy
-- ════════════════════════════════════════════════════

/-! After pragmatic strengthening (M-principle) the effective semantics uses
    a `ThresholdPair`; the four forms split via the borderline region. This
    is captured by substrate `AntonymForm.strengthenedDenot`. The synonymy
    breaking witness for `notNegative` vs `positive` under strict gap is the
    substrate theorem `strengthenedDenot_breaks_synonymy`. -/

-- ════════════════════════════════════════════════════
-- § 4. Concrete Scale: Happy/Unhappy on Degree 4
-- ════════════════════════════════════════════════════

/-- 5-point happiness scale (matching [tessler-franke-2019]'s model). -/
abbrev HappyDeg := Degree 4

/-- Contradictory boundary at θ = 2 (the literal semantics). -/
abbrev happyθ : Threshold 4 := thr 2

/-- Effective threshold pair after pragmatic strengthening:
    θ_pos = 2, θ_neg = 1. The gap [1, 2] is the plateau region where
    "not unhappy" lands. -/
def happyTP : ThresholdPair 4 where
  pos := thr 2
  neg := thr 1
  gap_exists := by decide

theorem happy_gap_strict :
    (happyTP.neg : HappyDeg) < (happyTP.pos : HappyDeg) := by decide

-- ════════════════════════════════════════════════════
-- § 5. Core Predictions
-- ════════════════════════════════════════════════════

/-- **Prediction 1**: Under contradictory semantics, all degrees are
    classified. No gap exists. (Krifka's H2: literal exhaustivity.) -/
theorem contradictory_exhaustive_concrete :
    ∀ d : HappyDeg, AntonymForm.contradictoryDenot happyθ .positive d ∨
                     AntonymForm.contradictoryDenot happyθ .negative d :=
  fun d => Decidable.em (positiveMeaning d happyθ)

/-- **Prediction 2**: Under contradictory semantics, "not unhappy" iff "happy"
    at every degree. (The puzzle.) -/
theorem contradictory_not_unhappy_iff_happy :
    ∀ d : HappyDeg, AntonymForm.contradictoryDenot happyθ .notNegative d ↔
                     AntonymForm.contradictoryDenot happyθ .positive d :=
  fun _ => Iff.rfl

/-- **Prediction 3**: After strengthening, the gap breaks synonymy. -/
theorem strengthened_not_unhappy_witnesses_gap :
    ∃ d : HappyDeg,
      AntonymForm.strengthenedDenot happyTP .notNegative d ∧
      ¬ AntonymForm.strengthenedDenot happyTP .positive d :=
  strengthenedDenot_breaks_synonymy happyTP happy_gap_strict

/-- **Prediction 4**: The gap region is nonempty (degrees 1 and 2). -/
theorem gap_degrees :
    inGapRegion (deg 1 : HappyDeg) happyTP ∧
    inGapRegion (deg 2 : HappyDeg) happyTP := by
  refine ⟨⟨?_, ?_⟩, ⟨?_, ?_⟩⟩ <;> decide

/-- **Prediction 5**: Degree 0 is "unhappy", degree 4 is "happy". -/
theorem extreme_degrees :
    contraryNegMeaning (deg 0 : HappyDeg) happyTP ∧
    positiveMeaning' (deg 4 : HappyDeg) happyTP :=
  ⟨by decide, by decide⟩

-- ════════════════════════════════════════════════════
-- § 6. Transfer: Tessler-Franke Quadruplet Rows
-- ════════════════════════════════════════════════════

/-! [tessler-franke-2019]'s quadruplet rows
(`Data/Examples/TesslerFranke2019.json`) classify each form by its
innermost negation (`inner_neg`), preferred interpretation
(`interpretation`), cost parameter (`cost`), and whether the form is judged
equivalent to the bare positive (`equivalent_to_positive`). The theorems
below are the transfer equations between those classifications and the
strengthened model (§ 4-5). -/

/-- Value of a `paperFeatures` key, if present. -/
def featureOf (row : LinguisticExample) (key : String) : Option String :=
  (row.paperFeatures.find? (·.1 == key)).map (·.2)

/-- Quadruplet-form adapter: the row's `form` feature as an `AntonymForm`. -/
def formOf (row : LinguisticExample) : Option AntonymForm :=
  match featureOf row "form" with
  | some "positive" => some .positive
  | some "notPositive" => some .notPositive
  | some "negative" => some .negative
  | some "notNegative" => some .notNegative
  | _ => none

/-- Interpretation adapter: the row's preferred `interpretation` feature as a
    `NegationType`. -/
def interpretationOf (row : LinguisticExample) : Option NegationType :=
  match featureOf row "interpretation" with
  | some "contrary" => some .contrary
  | some "contradictory" => some .contradictory
  | _ => none

/-- **Interpretation follows the innermost negation**: a row's preferred
    interpretation is contrary iff its innermost negation is morphological.
    "Not unhappy" inherits the contrary reading of the inner *un-* — the
    *effective* (post-strengthening) semantics, consistent with Krifka's
    analysis where the contrary behavior is pragmatically derived. -/
theorem interpretation_follows_inner_neg :
    ∀ row ∈ TesslerFranke2019.Examples.all,
      (featureOf row "inner_neg" = some "morphological" ↔
        interpretationOf row = some .contrary) := by
  decide

private instance {max : Nat} (tp : ThresholdPair max) (q : AntonymForm)
    (d : Degree max) : Decidable (AntonymForm.strengthenedDenot tp q d) := by
  cases q <;> dsimp [AntonymForm.strengthenedDenot] <;> infer_instance

/-- **Transfer equation**: a row is judged equivalent to the bare positive iff
    the strengthened denotation of its form coincides with `positive` at every
    degree. The load-bearing case is `notNegative` ("not unhappy"): the gap
    (§ 5, Prediction 3-4) puts it on the non-equivalent side. -/
theorem equivalent_to_positive_iff_strengthened :
    ∀ row ∈ TesslerFranke2019.Examples.all, ∀ f ∈ formOf row,
      (featureOf row "equivalent_to_positive" = some "true" ↔
        ∀ d : HappyDeg, (AntonymForm.strengthenedDenot happyTP f d ↔
          AntonymForm.strengthenedDenot happyTP .positive d)) := by
  decide

-- ════════════════════════════════════════════════════
-- § 7. Bridge to Markedness Infrastructure
-- ════════════════════════════════════════════════════

/-- Krifka's form complexity ordering matches the markedness infrastructure.
    "unhappy" is marked over "happy" by morphological complexity. -/
theorem unhappy_marked_by_morphology :
    Implicature.Markedness.computeMarked
      Implicature.Markedness.happy_with_morphology
      Implicature.Markedness.unhappy_with_morphology = some "unhappy" := by
  decide

/-- **Cost = complexity**: the rows' cost parameters (`C(un-) = 2`,
    `C(not) = 3`, additive) equal Krifka's form complexity
    (`AntonymForm.complexity`) at every quadruplet form. With
    `interpretation_follows_inner_neg` this is Horn's division of pragmatic
    labor: the cheaper single negation ("unhappy") takes the contrary
    reading, the costlier one ("not happy") the marked contradictory one. -/
theorem cost_eq_complexity :
    ∀ row ∈ TesslerFranke2019.Examples.all, ∀ f ∈ formOf row,
      featureOf row "cost" = some (toString f.complexity) := by
  decide

-- ════════════════════════════════════════════════════
-- § 8. Unconditionals: Evidence for Contradictory Base
-- ════════════════════════════════════════════════════

/-- Krifka's unconditional argument:
    "Regardless whether you are happy or unhappy, you should read this book."

    This sentence entails the predicate covers EVERYONE — no gap.
    Under the contradictory model, happy ∨ unhappy = universal (exhaustive).
    Under a contrary model, this would exclude the gap region.

    Unconditionals provide evidence that the literal semantics IS
    contradictory, with the gap being purely pragmatic. -/
theorem unconditional_exhaustive :
    ∀ d : HappyDeg, AntonymForm.contradictoryDenot happyθ .positive d ∨
                     AntonymForm.contradictoryDenot happyθ .negative d :=
  contradictory_exhaustive_concrete

/-- Contrast: the strengthened model does NOT exhaust the scale.
    There exist degrees in the gap that are neither "happy" nor "unhappy". -/
theorem strengthened_not_exhaustive :
    ∃ d : HappyDeg, ¬ AntonymForm.strengthenedDenot happyTP .positive d ∧
                     ¬ AntonymForm.strengthenedDenot happyTP .negative d :=
  ⟨deg 1, by decide, by decide⟩

-- ════════════════════════════════════════════════════
-- § 9. Bidirectional OT: Deriving the Quadruplet
-- ════════════════════════════════════════════════════

/-! [blutner-2000]'s weak Bidirectional OT (eq. 14, "weak optimality")
    derives the form-meaning assignment from constraint competition. Krifka
    explicitly invokes this version (p. 6, citing [blutner-2000] and
    [jaeger-2002]). The evaluation uses `superoptimal` from
    `Core.Optimization.Evaluation`.

    Two ranked constraints:
    1. **M-principle** ([horn-1984]): simple forms pair with stereotypical
       meanings; complex forms pair with non-stereotypical meanings.
    2. **Economy**: minimize form complexity.

    Under weak BiOT, the four-way form-meaning assignment emerges from the
    greatest-fixed-point computation regardless of ranking. This is because
    the weak BiOT fixed point re-admits pairs whose blockers were themselves
    eliminated — producing Horn's division of pragmatic labour in all cases
    where each form has a unique best meaning and vice versa. -/

/-- Meaning regions on the scale after pragmatic strengthening.
    The contradictory threshold θ splits into four regions:
    two stereotypical (clearly above/below) and two non-stereotypical
    (borderline, the plateau that becomes the "gap"). -/
inductive Region where
  | positive     -- clearly happy (d >> θ)
  | plateauHigh  -- borderline high: "not unhappy" territory
  | plateauLow   -- borderline low: "not happy" territory
  | negative     -- clearly unhappy (d << θ)
  deriving Repr, DecidableEq

/-- Semantically compatible form-meaning pairs as a `Finset` (decidable
    membership; supports `decide`-based per-pair blocking checks via the
    substrate's `Blocks.decidableOnFinset` instance).

    Under contradictory semantics (H2), forms partition by literal denotation:
    - Literally positive (happy, not unhappy): d > θ → positive or plateauHigh
    - Literally negative (unhappy, not happy): d ≤ θ → negative or plateauLow -/
def biotPairs : Finset (AntonymForm × Region) :=
  { (.positive,    .positive),    (.positive,    .plateauHigh),
    (.notNegative, .positive),    (.notNegative, .plateauHigh),
    (.negative,    .negative),    (.negative,    .plateauLow),
    (.notPositive, .negative),    (.notPositive, .plateauLow) }

/-- Krifka's predicted form-meaning quadruplet: each form gets a unique
    meaning region. The substrate-`superoptimalSet`-as-greatest-fixed-point
    formulation hits this set as the GFP under both M>>E and E>>M rankings
    (see `krifka_biot_prediction` and `economy_ranking_independent`). -/
def krifkaQuadruplet : Finset (AntonymForm × Region) :=
  { (.positive,    .positive), (.notNegative, .plateauHigh),
    (.negative,    .negative), (.notPositive, .plateauLow) }

/-- **M-Principle** constraint ([horn-1984], Horn's Division of Pragmatic
    Labor): penalizes mismatch between form complexity and meaning
    stereotypicality.
    - Simple form + stereotypical meaning → 0 violations (match)
    - Complex form + non-stereotypical meaning → 0 violations (match)
    - Cross-assignment → 1 violation (mismatch) -/
def mPrinciple : NamedConstraint (AntonymForm × Region) where
  name := "M-Principle"
  family := .markedness
  eval
    | (.positive,    .positive)    => 0  -- simple + stereotypical
    | (.positive,    .plateauHigh) => 1  -- simple + non-stereotypical
    | (.notNegative, .positive)    => 1  -- complex + stereotypical
    | (.notNegative, .plateauHigh) => 0  -- complex + non-stereotypical
    | (.negative,    .negative)    => 0
    | (.negative,    .plateauLow)  => 1
    | (.notPositive, .negative)    => 1
    | (.notPositive, .plateauLow)  => 0
    | _ => 0

/-- **Economy** constraint: penalizes form complexity.
    Violation count = `AntonymForm.complexity`. -/
def economyQ : NamedConstraint (AntonymForm × Region) where
  name := "Economy"
  family := .faithfulness
  eval p := p.1.complexity

/-- Build the violation profile for a ranking of constraints. -/
def biotProfile (ranking : List (NamedConstraint (AntonymForm × Region)))
    (p : AntonymForm × Region) : List Nat :=
  ranking.map fun c => c.eval p

/-- **Main BiOT result**: M-Principle >> Economy derives Krifka's quadruplet
    assignment. Each form gets a unique meaning region:
    - "happy" → clearly positive (stereotypical)
    - "not unhappy" → borderline positive / plateau (non-stereotypical)
    - "unhappy" → clearly negative (stereotypical)
    - "not happy" → borderline negative / plateau (non-stereotypical)

    Stated on the Finset-native canonical form `superoptimal` from the
    substrate; equality with the literal Finset is `decide`-checked
    directly. -/
theorem krifka_biot_prediction :
    superoptimal biotPairs (biotProfile [mPrinciple, economyQ]) =
      krifkaQuadruplet := by
  decide

/-- **Structural lift to abstract gfp form**: the same equality holds at
    the set-valued `OrderHom.gfp` level (via the substrate bridge theorem
    `superoptimal_coe_eq_set`). Useful for arguments that reason about
    the abstract gfp directly (e.g. universal properties of superoptimal
    sets, comparisons across BiOT variants). -/
theorem krifka_biot_prediction_gfp :
    superoptimalSet (↑biotPairs) (biotProfile [mPrinciple, economyQ]) =
      ↑krifkaQuadruplet := by
  rw [← superoptimal_coe_eq_set _ _ (by decide), krifka_biot_prediction]

/-- Each quadruplet form receives a unique meaning. Derives from the BiOT
    assignment via `Finset.image`. -/
theorem biot_covers_all_forms :
    krifkaQuadruplet.image Prod.fst =
      ({ .positive, .notNegative, .negative, .notPositive } :
        Finset AntonymForm) := by
  decide

/-- Each region is assigned to exactly one form. Derives from the BiOT
    assignment via `Finset.image`. -/
theorem biot_covers_all_regions :
    krifkaQuadruplet.image Prod.snd =
      ({ .positive, .plateauHigh, .negative, .plateauLow } :
        Finset Region) := by
  decide

/-- **Headline ranking-invariance**: under weak BiOT, Economy >> M produces
    the SAME four-way assignment as M >> Economy. The greatest-fixed-point
    computation re-admits the complex forms after their blockers are
    removed: pairs like ⟨notNegative, plateauHigh⟩ are initially blocked
    by ⟨positive, plateauHigh⟩, but that pair is itself blocked by
    ⟨positive, positive⟩, so ⟨notNegative, plateauHigh⟩ returns.

    This ranking-independence is a general property of weak BiOT for
    form-meaning games where each form has a unique best meaning. Under
    strong BiOT, Economy >> M would collapse the quadruplet to only two
    pairs.

    Direct kernel-verified `decide`: both Finset iterations stabilize to
    the same fixed point. -/
theorem economy_ranking_independent :
    superoptimal biotPairs (biotProfile [economyQ, mPrinciple]) =
    superoptimal biotPairs (biotProfile [mPrinciple, economyQ]) := by
  decide

/-- The full quadruplet survives under both rankings. -/
theorem both_rankings_give_quadruplet :
    superoptimal biotPairs (biotProfile [mPrinciple, economyQ]) =
      krifkaQuadruplet ∧
    superoptimal biotPairs (biotProfile [economyQ, mPrinciple]) =
      krifkaQuadruplet :=
  ⟨krifka_biot_prediction, by decide⟩

/-- The BiOT derivation agrees with the strengthened semantics (§ 3):
    "happy" and "not unhappy" are assigned to different meaning regions,
    breaking the synonymy that holds under contradictory semantics (§ 2). -/
theorem biot_breaks_synonymy :
    (.positive, Region.positive) ∈
      superoptimal biotPairs (biotProfile [mPrinciple, economyQ]) ∧
    (.notNegative, Region.plateauHigh) ∈
      superoptimal biotPairs (biotProfile [mPrinciple, economyQ]) := by
  rw [krifka_biot_prediction]
  refine ⟨?_, ?_⟩ <;> decide

end Krifka2007
