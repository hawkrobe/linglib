import Linglib.Studies.BarkerPullum1990
import Linglib.Core.Order.Branching

/-!
# Reinhart (1976) [reinhart-1976]

The Syntactic Domain of Anaphora. PhD dissertation, MIT.

## Key Contributions

1. **C-command** (def. 36, p. 32): replaces [langacker-1969]'s S-node-based
   "command" with a **branching-node-based** relation
2. **C-command domain** (def. 38, p. 33): the subtree dominated by the first
   branching node dominating A — always a constituent
3. **Coreference restriction** (10b, p. 14): domain-based, dispensing with "precede"
4. **Claim (49)** (p. 40): c-command ⊆ command (= `cCommand ⊆ sCommand` in B&P)
5. **The irrelevance of precede** (§1.4): linear order is epiphenomenal for coreference

## Connection to [barker-pullum-1990]

Reinhart's c-command is exactly B&P's **c-command** (parameterized by branching
nodes). [langacker-1969]'s command is B&P's **S-command** (parameterized by
S-nodes). Theorem 49 follows from B&P's antitone map: since
`{S-nodes} ⊆ {branching nodes}`, we get `C_{branching} ⊆ C_{S}`.

## Concrete verification over the position tower

The concrete c-command facts (§5-§7) use `Branching.cCommandAt` — the
B&P command relation over `Branching.toTreeOrder` of a concrete
`Syntax.Tree`, generated by the geometric branching nodes
(`Branching.isBranchingAt`, ≥ 2 daughters). In a binary tree every
non-leaf node branches, so "the first branching node dominating A" is
A's parent and A's parent dominates B iff A's sister dominates B —
[reinhart-1976]'s c-command. Membership is `decide`-checked via
`Branching.mem_commandRelation_toTreeOrder_iff`, which collapses the
defining universal to the finite list of A's prefixes.
-/

set_option autoImplicit false

namespace Reinhart1976

open BarkerPullum1990
open Core.Order
open Core.Order.Branching (isBranchingAt cCommandAt yield)
open Syntax (Tree)
open Syntax.Tree (leaf bin)
open Set

-- ============================================================================
-- §1: Reinhart's Definitions
-- ============================================================================

/-! ### Definition 1 (p. 8) — Langacker's "command"

> A node A commands a node B if neither A nor B dominates the other and
> the S node most immediately dominating A also dominates B.

This is B&P's S-command, parameterized by S-nodes (`Core.Order.commandRelation` with P = S-nodes). -/

/-! ### Definition 36 (p. 32) — C-command

> Node A c(onstituent)-commands node B if neither A nor B dominates the
> other and the first **branching node** which dominates A dominates B.

This is B&P's c-command (their Definition 10), generated by branching
nodes: `Core.Order.cCommand` abstractly, `Branching.cCommandAt` concretely.

Reinhart explicitly contrasts this with Langacker's command (p. 32):
"The difference between the relations of command and of c-command is
that while the first mentions **cyclic nodes** the second does not —
all **branching nodes** can be relevant." -/

/-! ### Definition 38 (p. 33) — C-command domain

> The domain of a node A consists of A together with all and only the
> nodes c-commanded by A. (OR: The domain of a node A is the subtree
> dominated by the first branching node which dominates A.)

A key observation (p. 34): c-command domains are always **constituents**
(subtrees), while precede-and-command domains may not be. -/

/-- The **c-command domain** of a node `a`: the set of nodes that `a`
    c-commands, plus `a` itself.

    In B&P terms: `{b | (a, b) ∈ cCommand T} ∪ {a}`. -/
def cCommandDomain {Node : Type} [PartialOrder Node] (T : TreeOrder Node) (a : Node) : Set Node :=
  {b | (a, b) ∈ cCommand T} ∪ {a}

-- ============================================================================
-- §2: Claim (49) — C-command ⊆ Command
-- ============================================================================

/-! ### Claim (49) (p. 40)

> A c-commands B ⟶ A commands B
> A does not command B ⟶ A does not c-command B

In B&P terms: `cCommand T ⊆ sCommand T`, provided every S-node is
also a branching node — a universally accepted structural assumption
(S-nodes always dominate both a subject and a predicate). -/

/-- **Claim (49)**: C-command implies command.

    Every S-node is a branching node (S-nodes dominate ≥2 children),
    so `{S-nodes} ⊆ {branching nodes}`, and by B&P's antitone map
    (`command_antitone`), `C_{branching} ⊆ C_{S}`. -/
theorem cCommand_implies_command {Node : Type} [PartialOrder Node] (T : LabeledTree Node)
    (h_S_branch : sNodes T ⊆ branchingNodes T.toTreeOrder) :
    cCommand T.toTreeOrder ⊆ sCommand T :=
  command_antitone T.toTreeOrder (sNodes T) (branchingNodes T.toTreeOrder) h_S_branch

-- ============================================================================
-- §3: The Coreference Restriction (10b)
-- ============================================================================

/-! ### Restriction 10b (p. 14)

> Two NP's in a non strict reflexive environment can be coreferential
> just in case **if either is in the domain of the other, the one in
> the domain is a pronoun**.

Reinhart argues (§1.4) that the earlier formulation using
precede-and-command is both empirically wrong (fails for preposed PPs)
and theoretically unnatural (c-command domains are constituents;
precede-and-command domains are not). -/

/-- Whether node `a` is a pronoun. -/
abbrev IsPronoun (Node : Type) := Node → Prop

/-- **Reinhart's Coreference Restriction (10b)**.

    Two nodes can corefer unless one is in the c-command domain of the
    other and is not a pronoun.

    `corefPermitted isPron T a b` holds iff:
    - neither is in the other's domain, OR
    - whichever is in the other's domain is a pronoun. -/
def corefPermitted {Node : Type} [PartialOrder Node] (isPron : IsPronoun Node)
    (T : TreeOrder Node) (a b : Node) : Prop :=
  (b ∈ cCommandDomain T a → isPron b) ∧
  (a ∈ cCommandDomain T b → isPron a)

-- ============================================================================
-- §4: The Irrelevance of Precede (§1.4)
-- ============================================================================

/-! ### The irrelevance of precede (§1.4, §1.5)

Reinhart's central argumentative contribution: the relation **precede** plays
no role in determining anaphora options. Two key observations:

1. **Preposed PPs** (§1.5.2): In "Near him, Dan saw a snake" (45),
   the pronoun *precedes* the antecedent yet coreference is fine —
   because "him" (PP-internal) does not c-command "Dan" (the subject).
   Meanwhile, "\*Near Dan, he saw a snake" (43a) is correctly blocked:
   "Dan" is in the c-command domain of "he" and is not a pronoun.

2. **VOS languages** (p. 41): In Malagasy, the pronoun precedes and
   commands the antecedent (by the precede-and-command definition)
   yet coreference is permitted — because the pronoun does not
   **c-command** the antecedent.

Both facts follow automatically from the c-command restriction (10b)
without mentioning linear order. -/

/-- **Precede is irrelevant**: c-command domains are symmetric with
    respect to linear order.

    In B&P terms: the command relation `C_P` is defined purely in terms
    of dominance (`upperBounds`, `dom`), with no reference to precedence.

    This is a structural fact about how `commandRelation` is defined —
    it uses only vertical (dominance) relations, never horizontal
    (precedence) relations. -/
theorem command_ignores_precedence {Node : Type} [PartialOrder Node] (T : TreeOrder Node) (P : Set Node) :
    ∀ a b, (a, b) ∈ commandRelation T P ↔
      ∀ x ∈ upperBounds T a P, x ≤ b :=
  λ _ _ => Iff.rfl

-- ============================================================================
-- §5: Concrete Verification over the position tower
-- ============================================================================

/-! ### C-command as `cCommandAt` over concrete trees

The concrete tests use the [barker-pullum-1990] tower:
[reinhart-1976]'s c-command is `cCommandAt t` — the B&P command relation
over the dominance order `Branching.toTreeOrder t` of a concrete
`Syntax.Tree`, generated by the geometric branching nodes
(`isBranchingAt`, ≥ 2 daughters). Positions are `TreePath`s (Gorn
addresses, `0` = left, `1` = right); membership is `decide`-checked via
`Branching.mem_commandRelation_toTreeOrder_iff`. This replaces the
earlier bespoke address/`cCommand` stack with the project-canonical
position machinery, making the c-command facts theorems about the same
command relation the rest of the library attributes to [reinhart-1976]
([reinhart-1976]'s c-command = B&P c-command). -/

/-- "John saw Mary": `[S [NP John] [VP [V saw] [NP Mary]]]`.
    Subject at `⟨[0]⟩`, V at `⟨[1,0]⟩`, object at `⟨[1,1]⟩`. -/
def johnSawMary : Tree Unit String :=
  bin (leaf "John") (bin (leaf "saw") (leaf "Mary"))

/-- The yield is the surface string, left to right. -/
theorem johnSawMary_yield : yield johnSawMary = ["John", "saw", "Mary"] := by
  simp only [johnSawMary, Branching.yield_def, Branching.children,
    HasContent.content?, Option.toList, List.flatMap_cons, List.flatMap_nil,
    List.nil_append, List.append_nil, List.cons_append, List.singleton_append]

/-- The subject `⟨[0]⟩` k-commands the object `⟨[1,1]⟩`: the only
    branching node dominating the subject is the root, which dominates
    everything. -/
theorem subject_ccommands_object :
    (⟨[0]⟩, ⟨[1, 1]⟩) ∈ cCommandAt johnSawMary := by decide

/-- The converse fails: the object `⟨[1,1]⟩` does **not** k-command the
    subject `⟨[0]⟩` — the VP node `⟨[1]⟩` dominates the object but not the
    subject. This is the subject–object asymmetry. -/
theorem object_not_ccommands_subject :
    (⟨[1, 1]⟩, ⟨[0]⟩) ∉ cCommandAt johnSawMary := by decide

/-- The subject c-commands into the VP (here, the verb `⟨[1,0]⟩`). -/
theorem subject_ccommands_into_vp :
    (⟨[0]⟩, ⟨[1, 0]⟩) ∈ cCommandAt johnSawMary := by decide

/-- Object and verb mutually k-command (co-arguments inside the VP). -/
theorem object_verb_mutual_ccommand :
    (⟨[1, 1]⟩, ⟨[1, 0]⟩) ∈ cCommandAt johnSawMary ∧
    (⟨[1, 0]⟩, ⟨[1, 1]⟩) ∈ cCommandAt johnSawMary := by
  constructor <;> decide

/-- C-command is not symmetric: subject k-commands object, not conversely. -/
theorem ccommand_asymmetric_example :
    (⟨[0]⟩, ⟨[1, 1]⟩) ∈ cCommandAt johnSawMary ∧
    (⟨[1, 1]⟩, ⟨[0]⟩) ∉ cCommandAt johnSawMary :=
  ⟨subject_ccommands_object, object_not_ccommands_subject⟩

/-! ### Preposed PP example (§1.5.2)

[reinhart-1976]'s structures (41)/(42) are **ternary branching**
(S → PP NP₁ VP), but the key half of the argument is tree-shape-independent:
the PP-internal NP ("Dan" inside "near Dan") does NOT c-command the
subject region, because the first branching node dominating it is PP,
which does not dominate the subject. The precede-and-command approach
wrongly blocks backward pronominalization in "Near him, Dan saw a
snake" (45); the c-command restriction correctly permits it. -/

/-- Preposed PP, binary encoding `[S [PP near Dan] [VP' [NP he] …]]`:
    NP_Dan at `⟨[0,1]⟩`, NP_he at `⟨[1,0]⟩`. Dan does **not** k-command
    the subject-region NP_he: the only branching node above Dan that also
    sits above the subject is the root, but PP `⟨[0]⟩` dominates Dan and
    not the subject, blocking the universal. -/
def nearDanHeSaw : Tree Unit String :=
  bin (bin (leaf "near") (leaf "Dan"))
      (bin (leaf "he") (bin (leaf "saw") (leaf "snake")))

theorem preposed_pp_no_ccommand :
    (⟨[0, 1]⟩, ⟨[1, 0]⟩) ∉ cCommandAt nearDanHeSaw := by decide

/-! ### Subject–object asymmetry with a complex subject

"The man who traveled with her denied that Rosa met the Shah": *her*
deep inside the subject does NOT c-command the embedded subject *Rosa*,
so coreference is permitted; whereas a bare matrix subject does
c-command the embedded subject. -/

/-- Complex-subject clause `[S [NP the man with x her] [VP denied [S' Rosa
    metShah]]]`: matrix subject `⟨[0]⟩`, embedded subject `⟨[1,1,0]⟩`,
    deep "her" `⟨[0,1,1,1,1]⟩`. -/
def complexSubject : Tree Unit String :=
  bin
    (bin (leaf "the") (bin (leaf "man") (bin (leaf "with") (bin (leaf "x") (leaf "her")))))
    (bin (leaf "denied") (bin (leaf "Rosa") (leaf "metShah")))

/-- The matrix subject k-commands the embedded subject. -/
theorem matrix_subject_ccommands_embedded :
    (⟨[0]⟩, ⟨[1, 1, 0]⟩) ∈ cCommandAt complexSubject := by decide

/-- "her" deep inside the subject does **not** k-command the embedded
    subject: every branching node dominating it stays inside the subject. -/
theorem deep_pronoun_not_ccommands_embedded :
    (⟨[0, 1, 1, 1, 1]⟩, ⟨[1, 1, 0]⟩) ∉ cCommandAt complexSubject := by decide

-- ============================================================================
-- §6: End-to-End Verification — The (11) Paradigm (pp. 14-15)
-- ============================================================================

/-! ### Coreference permission over the tower (decidable)

A decidable, `Prop`-valued form of `corefPermitted` (10b) for concrete
trees: coreference is permitted unless one NP k-commands the other and
the k-commanded NP is not a pronoun. -/

/-- **Coreference permission** (restriction 10b) over a concrete tree.

    `a, b` may corefer iff: if `a` k-commands `b` then `b` is a pronoun,
    and if `b` k-commands `a` then `a` is a pronoun. Omits the "non
    strict reflexive environment" qualification (non-reflexive coreference
    only), matching the abstract `corefPermitted`. -/
def corefPermittedAt (t : Tree Unit String) (isPronA isPronB : Prop)
    [Decidable isPronA] [Decidable isPronB] (a b : TreePath) : Prop :=
  ((a, b) ∈ cCommandAt t → isPronB) ∧ ((b, a) ∈ cCommandAt t → isPronA)

instance (t : Tree Unit String) (isPronA isPronB : Prop)
    [Decidable isPronA] [Decidable isPronB] (a b : TreePath) :
    Decidable (corefPermittedAt t isPronA isPronB a b) := by
  unfold corefPermittedAt; infer_instance

/-! ### The (11) paradigm (pp. 14-15)

Structure `[S NP₁ [VP denied [S' NP₂ [VP' has met the Shah]]]]`, NP₁ at
`⟨[0]⟩`, NP₂ at `⟨[1,1,0]⟩`. The matrix subject NP₁ k-commands NP₂ (not
vice versa), so (10b) requires NP₂ to be a pronoun for coreference:

- (11a) Rosa denied that Rosa met the Shah.  — **blocked**
- (11b) She denied that Rosa met the Shah.   — **blocked**
- (11c) Rosa denied that she met the Shah.   — **permitted**
- (11d) She denied that she met the Shah.    — **permitted** -/

/-- "NP₁ denied that NP₂ met the Shah": NP₁ at `⟨[0]⟩`, NP₂ at `⟨[1,1,0]⟩`. -/
def deniedParadigm : Tree Unit String :=
  bin (leaf "NP1") (bin (leaf "denied") (bin (leaf "NP2") (leaf "metShah")))

/-- Matrix subject k-commands embedded subject, not vice versa. -/
private theorem matrix_ccommands_embedded :
    (⟨[0]⟩, ⟨[1, 1, 0]⟩) ∈ cCommandAt deniedParadigm ∧
    (⟨[1, 1, 0]⟩, ⟨[0]⟩) ∉ cCommandAt deniedParadigm := by
  constructor <;> decide

/-- **(11a)** Rosa₁ denied that Rosa₂ met the Shah: coref **blocked**.
    Rosa₂ is in the domain of Rosa₁ and is not a pronoun. -/
theorem paradigm_11a_blocked :
    ¬ corefPermittedAt deniedParadigm False False ⟨[0]⟩ ⟨[1, 1, 0]⟩ := by decide

/-- **(11b)** She₁ denied that Rosa₂ met the Shah: coref **blocked**.
    Rosa₂ is in the domain of She₁ and is not a pronoun. -/
theorem paradigm_11b_blocked :
    ¬ corefPermittedAt deniedParadigm True False ⟨[0]⟩ ⟨[1, 1, 0]⟩ := by decide

/-- **(11c)** Rosa₁ denied that she₂ met the Shah: coref **permitted**.
    she₂ is in the domain of Rosa₁ but IS a pronoun. -/
theorem paradigm_11c_permitted :
    corefPermittedAt deniedParadigm False True ⟨[0]⟩ ⟨[1, 1, 0]⟩ := by decide

/-- **(11d)** She₁ denied that she₂ met the Shah: coref **permitted**.
    she₂ is in the domain of She₁ but IS a pronoun. -/
theorem paradigm_11d_permitted :
    corefPermittedAt deniedParadigm True True ⟨[0]⟩ ⟨[1, 1, 0]⟩ := by decide

-- ============================================================================
-- §7: (10a) vs (10b) — The Superiority of the Domain Formulation
-- ============================================================================

/-! ### Restriction (10a) — the pronoun-specific formulation (p. 14)

> Two NP's in a non strict reflexive environment can be coreferential
> just in case one is a pronoun, the other is not and the
> non-pronoun is not in the domain of the pronoun.

(10a) applies only to pronoun–full NP pairs; [reinhart-1976] argues (pp.
14-17) that (10b) is strictly superior — (10a) fails to block
coreference between two full NPs when one is in the domain of the other
(the (11a) case). -/

/-- **Restriction (10a)**: applies only to pronoun–full NP pairs. When
    exactly one NP is a pronoun, the non-pronoun must not be in the
    c-command domain of the pronoun; otherwise (10a) does not apply. -/
def corefPermittedAt_10a (t : Tree Unit String) (isPronA isPronB : Prop)
    [Decidable isPronA] [Decidable isPronB] (a b : TreePath) : Prop :=
  if isPronA ∧ ¬ isPronB then (a, b) ∉ cCommandAt t
  else if ¬ isPronA ∧ isPronB then (b, a) ∉ cCommandAt t
  else True

instance (t : Tree Unit String) (isPronA isPronB : Prop)
    [Decidable isPronA] [Decidable isPronB] (a b : TreePath) :
    Decidable (corefPermittedAt_10a t isPronA isPronB a b) := by
  unfold corefPermittedAt_10a; infer_instance

/-- **Non-equivalence of (10a) and (10b)**: (10a) fails on the (11a) case.
    (10a) cannot block coreference between two full NPs (Rosa₁ … Rosa₂),
    because it only applies to pronoun–full NP pairs; (10b) correctly
    blocks it because Rosa₂ is in the domain of Rosa₁ and is not a
    pronoun. -/
theorem restriction_10a_vs_10b :
    corefPermittedAt_10a deniedParadigm False False ⟨[0]⟩ ⟨[1, 1, 0]⟩ ∧
    ¬ corefPermittedAt deniedParadigm False False ⟨[0]⟩ ⟨[1, 1, 0]⟩ := by
  constructor <;> decide

/-- **(10b) subsumes (10a)**: whenever (10a) blocks coreference, (10b)
    does too. The converse fails (`restriction_10a_vs_10b`), so (10b) is
    strictly more restrictive. -/
theorem restriction_10b_subsumes_10a (t : Tree Unit String)
    (isPronA isPronB : Prop) [Decidable isPronA] [Decidable isPronB]
    (a b : TreePath)
    (h : ¬ corefPermittedAt_10a t isPronA isPronB a b) :
    ¬ corefPermittedAt t isPronA isPronB a b := by
  unfold corefPermittedAt_10a at h
  rintro ⟨hab, hba⟩
  split_ifs at h with h1 h2
  · exact h (fun hkc => absurd (hab hkc) h1.2)
  · exact h (fun hkc => absurd (hba hkc) h2.1)
  · exact h trivial

end Reinhart1976
