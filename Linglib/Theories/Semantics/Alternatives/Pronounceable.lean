import Linglib.Core.Tree

/-!
# Pronounceability of expressions

A predicate `pronounceable : S → Prop` separating expressions whose
phonological form is realizable in a given language from those that
are not. Most expressions in linglib are uniformly pronounceable,
but several pragmatic accounts require referring to silent competitors:

- @cite{jeretic-bassi-gonzalez-yatsushiro-meyer-sauerland-2025} —
  the dual-marked structure `tous les NP.dual` is generated by the
  grammar of French but blocked from pronunciation by Avoid Ambiguity;
  the indirect alternative `les deux NP` competes with it.
- @cite{sauerland-alexiadou-2020} — Meaning First posits thought-level
  alternatives that need not be articulated.
- Trace theory, ellipsis, sluicing — silent material more generally.

This module supplies the typeclass and a default instance making
`Tree C W` uniformly pronounceable, plus a lexicon-relativized
pronounceability predicate that flags trees containing core-concept
leaves not realized in the lexicon.

Pragmatic operators (e.g. `Indirect.indirectFrom`) require this
predicate to define alternatives that pass through unpronounceable
witnesses.
-/

set_option autoImplicit false

namespace Alternatives

universe u

/-- A type comes equipped with a pronounceability predicate.

The default instance (provided here for `Tree C W`) treats every
expression as pronounceable. Specialized instances can refine this
in lexicon-sensitive ways. -/
class HasPronounceability (S : Type u) where
  pronounceable : S → Prop

export HasPronounceability (pronounceable)

/-- Default: every tree is pronounceable. Override locally with a
`HasPronounceability` term in scope when needed. -/
instance defaultTreePronounceability {C W : Type} :
    HasPronounceability (Core.Tree.Tree C W) :=
  ⟨fun _ => True⟩

/-- Construct a `HasPronounceability` instance from an arbitrary
predicate. Useful when pronounceability is parameterized by external
data (e.g. a lexicon) that the typeclass system cannot synthesize. -/
@[reducible] def HasPronounceability.ofPred {S : Type u} (p : S → Prop) :
    HasPronounceability S := ⟨p⟩

end Alternatives
