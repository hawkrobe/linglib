import Linglib.Core.Computability.Subregular.Basic

/-!
# Strictly Piecewise Languages (SP_k)

A language `L` is **strictly `k`-piecewise** when membership is determined by
which length-`k` *subsequences* (scattered, non-contiguous selections) the
input contains @cite{rogers-pullum-2011} @cite{lambert-2022}. Whereas SL_k
constrains adjacent material via *factors* (contiguous infixes), SP_k
constrains long-distance co-occurrence via *subsequences*.

## Why subsequences (not factors)

The two locality dimensions in the subregular hierarchy are *adjacency-based*
(SL/LT/LTT, captured by contiguous `k`-factors) and *precedence-based*
(SP/SP-tier, captured by `k`-element subsequences). A long-distance
phonological pattern such as sibilant harmony — *sasi* but not *saʃi* — is
naturally SP_2: the forbidden length-2 subsequence is `[s, ʃ]` (and its
reverse), regardless of intervening material.

No boundary augmentation is needed for SP: subsequences are blind to position
and to symbols not in the subsequence itself, so leading/trailing markers add
no information. This is the canonical convention in Heinz's SP work
@cite{heinz-rogers-2010}.

## This file

- `SPGrammar k α` — a positive `k`-piecewise grammar over alphabet `α`.
- `SPGrammar.lang` — the `Language α` it generates: every length-`k`
  subsequence of `w` must be permitted.
- `IsSPk` — the property that a `Language α` is strictly `k`-piecewise.

The relation `List.Sublist` is mathlib's "is a (non-contiguous) subsequence
of" — exactly the relation we need.
-/

namespace Core.Computability.Subregular

universe u

variable {α : Type u}

-- ============================================================================
-- § 1: Strictly piecewise grammar
-- ============================================================================

/-- A **strictly `k`-piecewise grammar** over `α`: a set of *permitted*
    length-`k` subsequences. Unlike SL grammars, no boundary alphabet is
    used — subsequences are insensitive to position. -/
structure SPGrammar (k : ℕ) (α : Type u) where
  /-- The set of permitted length-`k` subsequences. -/
  permitted : Set (List α)

namespace SPGrammar

variable {k : ℕ}

/-- The language generated by an SP grammar: strings whose every length-`k`
    subsequence is permitted. -/
def lang (G : SPGrammar k α) : Language α := fun w =>
  ∀ s, s.length = k → List.Sublist s w → s ∈ G.permitted

@[simp] theorem mem_lang (G : SPGrammar k α) (w : List α) :
    w ∈ G.lang ↔ ∀ s, s.length = k → List.Sublist s w → s ∈ G.permitted :=
  Iff.rfl

-- ============================================================================
-- § 2: Trivial SP grammars
-- ============================================================================

/-- The SP grammar permitting *all* subsequences — accepts every string. -/
def univ (k : ℕ) (α : Type u) : SPGrammar k α := ⟨Set.univ⟩

@[simp] theorem lang_univ (k : ℕ) :
    (univ k α).lang = (Set.univ : Language α) := by
  funext w
  exact propext ⟨fun _ => trivial, fun _ _ _ _ => trivial⟩

end SPGrammar

-- ============================================================================
-- § 3: SP_k as a property of `Language α`
-- ============================================================================

/-- A language `L` is **strictly `k`-piecewise** iff some `SPGrammar k α`
    generates exactly `L`. -/
def IsSPk (k : ℕ) (L : Language α) : Prop :=
  ∃ G : SPGrammar k α, G.lang = L

/-- Every SP grammar witnesses SP_k for its language. -/
theorem SPGrammar.isSPk_lang {k : ℕ} (G : SPGrammar k α) : IsSPk k G.lang :=
  ⟨G, rfl⟩

end Core.Computability.Subregular
