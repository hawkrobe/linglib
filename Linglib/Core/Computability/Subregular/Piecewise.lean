/-
Copyright (c) 2026 Robert Hawkins. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Robert Hawkins
-/
import Linglib.Core.Computability.Subregular.Basic

/-!
# Strictly Piecewise Languages (SP_k)

A language `L` is **strictly `k`-piecewise** when membership is determined
by which length-`k` *subsequences* (scattered, non-contiguous selections)
the input contains @cite{rogers-pullum-2011} @cite{lambert-2022}. Whereas
SL_k constrains adjacent material via *factors* (contiguous infixes),
SP_k constrains long-distance co-occurrence via *subsequences*.

## Why subsequences (not factors)

The two locality dimensions in the subregular hierarchy are
*adjacency-based* (SL/LT/LTT, captured by contiguous `k`-factors) and
*precedence-based* (SP/SP-tier, captured by `k`-element subsequences).
A long-distance phonological pattern such as sibilant harmony — *sasi*
but not *saʃi* — is naturally SP_2: the forbidden length-2 subsequence
is `[s, ʃ]` (and its reverse), regardless of intervening material.

No boundary augmentation is needed for SP: subsequences are blind to
position and to symbols not in the subsequence itself, so leading and
trailing markers add no information. This is the canonical convention
in Heinz's SP work @cite{heinz-rogers-2010}.

## Main definitions

* `SPGrammar k α` — a positive `k`-piecewise grammar over alphabet `α`.
* `SPGrammar.lang` — the `Language α` it generates: every length-`k`
  subsequence of `w` must be permitted.
* `IsStrictlyPiecewise k L` — the property that a `Language α` is
  strictly `k`-piecewise.

The relation `List.Sublist` (`<+`) is mathlib's "is a (non-contiguous)
subsequence of" — exactly what SP needs.
-/

namespace Core.Computability.Subregular

open List

variable {α : Type*}

/-- A **strictly `k`-piecewise grammar** over `α`: a set of *permitted*
length-`k` subsequences. Unlike SL grammars, no boundary alphabet is
used — subsequences are insensitive to position. -/
@[ext]
structure SPGrammar (k : ℕ) (α : Type*) where
  /-- The set of permitted length-`k` subsequences. -/
  permitted : Set (List α)

namespace SPGrammar

variable {k : ℕ}

instance : Inhabited (SPGrammar k α) := ⟨⟨Set.univ⟩⟩

/-- The language generated by an SP grammar: strings whose every
length-`k` subsequence is permitted. -/
def lang (G : SPGrammar k α) : Language α := fun w =>
  ∀ s, s.length = k → s <+ w → s ∈ G.permitted

@[simp] lemma mem_lang (G : SPGrammar k α) (w : List α) :
    w ∈ G.lang ↔ ∀ s, s.length = k → s <+ w → s ∈ G.permitted :=
  Iff.rfl

end SPGrammar

/-- A language `L` is **strictly `k`-piecewise** iff some `SPGrammar k α`
generates exactly `L`. -/
def IsStrictlyPiecewise (k : ℕ) (L : Language α) : Prop :=
  ∃ G : SPGrammar k α, G.lang = L

/-- Every SP grammar witnesses `IsStrictlyPiecewise` for its language. -/
lemma SPGrammar.isStrictlyPiecewise_lang {k : ℕ} (G : SPGrammar k α) :
    IsStrictlyPiecewise k G.lang :=
  ⟨G, rfl⟩

end Core.Computability.Subregular
